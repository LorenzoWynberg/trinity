# Phase 1: Understand

## Required
1. Fetch story details using the endpoint from main prompt
2. Read `CLAUDE.md` for project rules

## Story Fields
- `title` - what to do
- `intent` - why it matters
- `description` - implementation context (if present)
- `acceptance` - done when ALL criteria met

## Optional Reading (check if applicable)
| Doc | When |
|-----|------|
| `docs/ARCHITECTURE.md` | System design questions |
| `docs/knowledge/` | Feature patterns |
| `docs/gotchas/` | Known pitfalls |

## If Retry (attempt > 1)
Check `git log` and `git diff` for previous work. Focus on feedback section.

## Done When
You can explain what to build and why.
